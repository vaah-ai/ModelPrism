# API Surface

> This is a preliminary API surface. Full OpenAPI spec will be generated by FastAPI auto-docs.
>
> **Dashboard REST endpoints** follow JSON:API 1.0 format (`application/vnd.api+json`).
> **Agent registration** follows JSON:API. **Agent WebSocket messages** use a lightweight custom format.
> **Auth endpoints** use standard `application/json` (exception for sidebase compatibility).
> **OpenAI proxy** endpoints use OpenAI format (standard JSON).

## Base URL

- Cloud: `https://api.modelprism.io`
- Self-hosted: `http://<your-server>:8000`

## Authentication

- **Dashboard API:** JWT Bearer token (from login)
- **Agent API:** Agent token (`mp_` prefix) in `Authorization` header
- **Public API Proxy:** OpenAI-compatible `sk-` keys in `Authorization: Bearer sk-...`

---

## Agent Endpoints

### POST /api/agents/register
Register a new GPU server agent.

**Request:**
```json
{
  "token": "mp_abc123...",
  "hostname": "gpu-node-1",
  "gpus": [
    {
      "name": "NVIDIA A100-SXM4-80GB",
      "memory_total_mb": 81200,
      "driver_version": "550.54.15",
      "cuda_version": "12.4"
    }
  ],
  "cpu": {
    "model": "AMD EPYC 7V12 64-Core",
    "cores": 64,
    "total_memory_gb": 512
  },
  "disk": {
    "total_gb": 2048,
    "available_gb": 1500
  },
  "os": "Ubuntu 22.04",
  "kernel": "5.15.0-generic",
  "agent_version": "0.1.0"
}
```

**Response:** `201 Created`
```json
{
  "agent_id": "ag_xyz789",
  "name": "cyan-koala-42",
  "ws_url": "wss://api.modelprism.io/ws/agents/ag_xyz789",
  "config": {
    "poll_interval_seconds": 2,
    "heartbeat_interval_seconds": 15
  }
}
```

---

### WebSocket /ws/agents/{agent_id}

Bidirectional WebSocket for agent communication.

**Agent → Backend (metrics):**
```json
{
  "type": "metrics",
  "ts": 1717000000,
  "gpu": [
    {
      "index": 0,
      "util_pct": 87,
      "mem_used_mb": 42100,
      "mem_total_mb": 81200,
      "temp_c": 72,
      "power_w": 285
    }
  ],
  "gpu_cache_pct": 62.5,
  "ram_used_gb": 128,
  "ram_total_gb": 512,
  "cpu_pct": 12.5,
  "load_1": 8.2,
  "load_5": 7.1,
  "load_15": 6.5,
  "disk_used_gb": 548,
  "disk_total_gb": 2048,
  "disk_pct": 26.8
}
```

**Agent → Backend (vLLM metrics per instance):**
```json
{
  "type": "vllm_metrics",
  "instance_id": "inst_001",
  "ts": 1717000000,
  "model_name": "Qwen2.5-72B-Instruct",
  "running": 3,
  "waiting": 2,
  "total_requests": 15234,
  "prompt_tokens_total": 450000000,
  "gen_tokens_total": 120000000,
  "ttft_p50_ms": 280,
  "ttft_p99_ms": 850,
  "gpu_cache_pct": 62.5,
  "tps": 1850,
  "prefix_cache_hit_pct": 34,
  "error_pct": 0.02,
  "trunc_pct": 1.2,
  "server_start_ts": 1716800000
}
```

**Backend → Agent (commands):**
```json
{
  "type": "command",
  "command_id": "cmd_001",
  "command": "deploy_model",
  "params": {
    "model_id": "Qwen/Qwen2.5-72B-Instruct",
    "huggingface_token": "hf_...",
    "vllm_params": {
      "tensor_parallel_size": 4,
      "quantization": "fp8",
      "max_model_len": 32768,
      "gpu_memory_utilization": 0.9,
      "port": 8001,
      "served_model_name": "qwen2.5-72b"
    }
  }
}
```

**Agent → Backend (command progress):**
```json
{
  "type": "command_progress",
  "command_id": "cmd_001",
  "status": "downloading",
  "progress_pct": 45,
  "message": "Downloading model weights... (2.4 GB / 5.3 GB)"
}
```

**Agent → Backend (command result):**
```json
{
  "type": "command_result",
  "command_id": "cmd_001",
  "status": "success",
  "result": {
    "instance_id": "inst_001",
    "port": 8001,
    "pid": 12345,
    "endpoint": "http://localhost:8001/v1"
  }
}
```

### Agent Heartbeat
Agent sends every 15 seconds:
```json
{
  "type": "heartbeat",
  "ts": 1717000000,
  "agents_running": ["vllm:5", "vllm:6"]
}
```

Backend should respond to keep connection alive. Backend can force agent to reconnect (for updates) by returning a reconnect signal.

---

## Dashboard API Endpoints (Frontend → Backend)

### Authentication

| Method | Path | Description |
|--------|------|-------------|
| POST | /api/auth/register | Register new user |
| POST | /api/auth/login | Login, returns JWT |
| POST | /api/auth/refresh | Refresh JWT token |
| POST | /api/auth/logout | Invalidate session |
| GET  | /api/auth/me | Current user info |
| PUT  | /api/auth/me | Update profile |

### Agent Management

| Method | Path | Description |
|--------|------|-------------|
| GET    | /api/agents | List all registered GPU servers |
| GET    | /api/agents/{id} | Single agent details |
| PUT    | /api/agents/{id} | Update agent name, settings, hourly pricing |
| DELETE | /api/agents/{id} | Deregister agent |
| POST   | /api/agents/tokens | Generate new agent registration token |

### Agent Logs

| Method | Path | Description |
|--------|------|-------------|
| POST   | /api/agents/{id}/logs | Agent pushes log entry (internal) |
| GET    | /api/agents/{id}/logs | Fetch agent logs with filters (dashboard) |
| Query params: `level=error|warning|info|debug`, `since=<ISO>`, `until=<ISO>`, `limit` |

### Live Metrics (WebSocket)

| Path | Description |
|------|-------------|
| /ws/dashboard/{agent_id} | Real-time metrics stream for a specific GPU server |
| /ws/dashboard | Aggregate metrics for all user's agents |

### Historical Metrics

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/metrics/{agent_id} | Historical metrics with time range |
| Query params: `range=1h\|6h\|1d\|1w\|1m`, `instance_id` |

### Model Management

| Method | Path | Description |
|--------|------|-------------|
| GET    | /api/models | List all deployed models across agents |
| POST   | /api/models/deploy | Deploy a new model (sends command to agent) |
| GET    | /api/models/{id} | Single model details |
| POST   | /api/models/{id}/stop | Stop model gracefully |
| POST   | /api/models/{id}/restart | Restart model |
| DELETE | /api/models/{id} | Delete/undeploy model |
| GET    | /api/models/{id}/logs | Model logs |
| POST   | /api/models/{id}/config | Update model config (if hot-swappable) |

### HuggingFace Hub Browser

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/hub/search | Search HF Hub models |
| Query params: `q, task, library, author, sort, limit, page` |
| GET | /api/hub/models/{model_id} | Get model details from HF |
| POST | /api/hub/estimate-vram | Estimate VRAM requirements for a model config |

### Benchmarks

| Method | Path | Description |
|--------|------|-------------|
| GET    | /api/benchmarks | List benchmark runs (with filters) |
| POST   | /api/benchmarks | Trigger new benchmark (sends command to agent) |
| GET    | /api/benchmarks/{id} | Single benchmark results |
| DELETE | /api/benchmarks/{id} | Delete benchmark run |
| GET    | /api/benchmarks/compare | Compare multiple benchmark runs |

### API Keys

| Method | Path | Description |
|--------|------|-------------|
| GET    | /api/keys | List API keys |
| POST   | /api/keys | Generate new API key |
| DELETE | /api/keys/{id} | Revoke API key |
| PATCH  | /api/keys/{id} | Update key (rate limits, scopes) |

### Usage & Billing

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/usage | Usage stats (per-key, per-model, per-user) |
| GET | /api/usage/export | Export usage as CSV/JSON |
| POST | /api/billing/checkout | Stripe checkout session |
| GET | /api/billing/subscription | Current subscription |
| GET | /api/billing/invoices | Invoice history |
| GET | /api/billing/usage | Stripe meter usage |

### User Management

| Method | Path | Description |
|--------|------|-------------|
| GET    | /api/workspace | Current workspace info |
| PUT    | /api/workspace | Update workspace |
| POST   | /api/workspace/invite | Invite user to workspace |
| GET    | /api/workspace/members | List workspace members |
| PUT    | /api/workspace/members/{id}/role | Change member role |
| DELETE | /api/workspace/members/{id} | Remove member |

### Admin

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/admin/users | List all users (admin only) |
| GET | /api/admin/servers | System-wide agent overview |
| GET | /api/admin/usage | System-wide usage statistics |

---

## Public API Proxy Endpoints

These match the OpenAI API format exactly:

| Method | Path | Description |
|--------|------|-------------|
| POST | /v1/chat/completions | OpenAI-compatible chat completions |
| POST | /v1/completions | OpenAI-compatible completions |
| GET  | /v1/models | List available models |
| POST | /v1/embeddings | Embeddings (if supported by backend model) |

**Auth:** `Authorization: Bearer sk-...`

**Usage tracking:** Every response includes usage metadata. Streaming responses count tokens incrementally. Costs are accrued per-request based on prompt/completion tokens × model rate.
